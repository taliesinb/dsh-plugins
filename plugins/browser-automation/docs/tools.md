# Tool reference

<!-- GENERATED by scripts/gen-tool-docs.mjs from curated-tools.mjs — do not edit by hand. -->

44 tools, registered into every eligible chat and subagent. Window-bound tools take an optional `windowId` (`s:<session>:<window>` for Safari, `c:<session>:<window>` for Chrome); when omitted the browser must have zero or one window open in the calling session — zero opens one (the result says `opened`), one is used, more is an error listing the open ids. Ids are validated against the caller's session. Nothing is spawned until a tool needs it; a session's windows close after `idleMinutes` without a tool call, on agent disposal, and on plugin unload.

## Index

- **Windows**: [`safari_open`](#safari_open), [`safari_close`](#safari_close), [`chrome_open`](#chrome_open), [`chrome_close`](#chrome_close)
- **Navigation and reading**: [`safari_navigate`](#safari_navigate), [`safari_get_page_content`](#safari_get_page_content), [`safari_get_page_structure`](#safari_get_page_structure), [`safari_wait_for`](#safari_wait_for), [`safari_get_youtube_notes`](#safari_get_youtube_notes), [`chrome_navigate`](#chrome_navigate), [`chrome_snapshot`](#chrome_snapshot), [`chrome_get_page_content`](#chrome_get_page_content), [`chrome_get_page_structure`](#chrome_get_page_structure), [`chrome_wait_for`](#chrome_wait_for)
- **JavaScript**: [`safari_evaluate_expression`](#safari_evaluate_expression), [`safari_evaluate_function`](#safari_evaluate_function), [`chrome_evaluate_function`](#chrome_evaluate_function), [`chrome_evaluate_expression`](#chrome_evaluate_expression)
- **Interaction**: [`safari_interact`](#safari_interact), [`safari_click`](#safari_click), [`safari_hover`](#safari_hover), [`safari_press_key`](#safari_press_key), [`safari_type_text`](#safari_type_text), [`chrome_click`](#chrome_click), [`chrome_fill`](#chrome_fill), [`chrome_fill_form`](#chrome_fill_form), [`chrome_hover`](#chrome_hover), [`chrome_press_key`](#chrome_press_key), [`chrome_type_text`](#chrome_type_text), [`chrome_interact`](#chrome_interact)
- **Screenshots**: [`safari_get_screenshot`](#safari_get_screenshot), [`safari_save_screenshot`](#safari_save_screenshot), [`chrome_get_screenshot`](#chrome_get_screenshot), [`chrome_save_screenshot`](#chrome_save_screenshot)
- **Diagnostics, dialogs and viewport**: [`safari_console_messages`](#safari_console_messages), [`safari_network_requests`](#safari_network_requests), [`safari_get_network_request`](#safari_get_network_request), [`safari_handle_dialog`](#safari_handle_dialog), [`safari_set_viewport_size`](#safari_set_viewport_size), [`chrome_console_messages`](#chrome_console_messages), [`chrome_network_requests`](#chrome_network_requests), [`chrome_get_network_request`](#chrome_get_network_request), [`chrome_handle_dialog`](#chrome_handle_dialog), [`chrome_set_viewport_size`](#chrome_set_viewport_size)

## Windows

### `safari_open`

Open a NEW Safari (Technology Preview) window private to this chat and return its windowId (s:<session>:<window>). Each window is an independent automation session (own tabs, cookies, JS state) in its own STP window, labeled with this chat in its banner; the user's regular Safari is never touched. Call again for additional independent windows. Optionally navigate to url immediately. Other safari_* tools auto-open a window when this session has none, so this is only needed for a second window or to get the id explicitly.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string |  | Optional URL to load in the new window. |

**Returns:** `{ windowId, url?, title? }`

**Notes:** Each Safari window is a separate `safaridriver --mcp` process: its own automation session, cookies and JS state, in its own STP window whose banner reads `This window is controlled by DSH: ‹chat› · s:0:1.` Nothing is shared between windows.

### `safari_close`

Close one Safari window of this chat (windowId) or all of them (omit). Frees the STP window and its process.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text: which ids were closed

### `chrome_open`

Open a NEW Chrome page (window) private to this chat and return its windowId (c:<session>:<window>). All Chrome windows of this chat share one isolated Chrome instance (fresh temporary profile: no saved logins; cookies shared between this chat's windows). Other chrome_* tools auto-open a window when this session has none.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string |  | URL to load (default about:blank). |

**Returns:** `{ windowId, url? }`

**Notes:** All Chrome windows of a session are pages of one isolated Chrome instance (fresh temporary profile; cookies shared between the session's windows). Chrome launches with the first page and quits with the last.

### `chrome_close`

Close one Chrome window of this chat (windowId) or all of them (omit). Chrome itself quits when the chat's last window closes.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text: which ids were closed

## Navigation and reading

### `safari_navigate`

Load a URL in this chat's Safari window and wait for the navigation to finish. Returns the loaded page's title and URL; read the page with safari_get_page_content.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string | yes | URL to load. |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** `{ windowId, url, title? }`

### `safari_get_page_content`

Read a web page with Safari's engine and return its content. TWO MODES. (1) With url and no windowId: reads in an ISOLATED pooled reader window, never this chat's own window, so a page you are working on is not disturbed — the default for "read this URL" (use instead of web_fetch for JavaScript-rendered pages or when web_fetch returns nothing). (2) With windowId, or with no url: reads the current page of this chat's Safari window (url, if also given, is loaded there first); node UIDs in the result can be used with safari_interact. Formats: markdown | webkitMarkdown | plainText | text | textTree | html | json, default markdown — the plugin's own DOM serializer (headings, code fences, inline code, tables, nested lists; identical in Safari and Chrome); webkitMarkdown and the rest are WebKit's own extraction. WebKit extracts only RENDERED text: closed <details>, collapsed accordions and unselected tab panels are omitted unless expand is on (default in isolated mode; the header reports what was expanded, or what stayed collapsed). Narrow a read with section (one heading's section, e.g. "#troubleshooting"), selectors (CSS), or scope: "main"; safari_get_page_structure lists the available headings/selectors. prepare runs a JS function body BEFORE extraction (to reveal content); script runs AFTER and returns its value as scriptResult. For YouTube videos use safari_get_youtube_notes.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string |  | URL to read. Without windowId this uses an isolated reader. |
| `windowId` | string |  | Read this chat's Safari window (s:<session>:<window>) instead of an isolated reader. Omit windowId AND url to read this session's single open window. |
| `waitMs` | number |  | Extra wait after load before extracting, for lazily rendered pages (default 0). |
| `format` | `markdown` \| `webkitMarkdown` \| `plainText` \| `text` \| `textTree` \| `html` \| `json` |  | Extraction format (default markdown = the plugin's serializer). webkitMarkdown is WebKit's markdown (no heading levels, no code fences); plainText is smallest; textTree/json carry roles, labels and node UIDs; html is the rendered DOM. |
| `expand` | boolean |  | Before extracting: open every <details>, click aria-expanded="false" accordions (outside nav/header/footer, never menus), and click through each tab group, appending the other panels' text under "Hidden tab panels". Default true for isolated reads, false for windows (it changes the page state). |
| `section` | string |  | CSS selector of ONE heading (e.g. "#troubleshooting" or "h2:nth-of-type(3)"); returns that heading through the next heading of the same or a higher level. Get selectors from safari_get_page_structure. |
| `selectors` | array of string |  | CSS selectors; only the matching subtrees are extracted. Isolated pages are edited in place, windows are hidden-and-restored. Not combinable with section. |
| `scope` | `auto` \| `main` \| `page` |  | "main": extract only the main landmark (main / [role=main] / article / #content); "page": everything; "auto" (isolated default): main when it holds >= 60% of the text, else page. Window default: page. Ignored when section/selectors are given. |
| `markHeadings` | boolean |  | For webkitMarkdown only: prefix headings with "## " so WebKit's output carries heading levels. Isolated only; default true for webkitMarkdown. (markdown has native headings.) |
| `clean` | boolean |  | Strip empty images/links and blank runs from markdown output (default true for markdown / webkitMarkdown). |
| `maxWordsPerParagraph` | number |  | Truncate every paragraph — code blocks included — beyond this many words (default 2000 = effectively none). Values below ~50 are for skimming structure only. |
| `includeURLs` | boolean |  | Include link/image URLs (default true). |
| `nodeIds` | `none` \| `editable` \| `interactive` \| `allContainers` |  | Which nodes get UIDs for safari_interact (window mode; default interactive). |
| `prepare` | string |  | JS function body run in the page BEFORE expand/scope/extraction (e.g. dismiss a cookie banner, click "show more"); use `return` to get a value back as prepareResult. |
| `script` | string |  | JS function body run in the page AFTER extraction; use `return`. Returned as scriptResult. |

**Returns:** `{ mode, windowId?, url, title?, format, content, notes[], expanded?{details,buttons,tabs}, collapsed?{details,buttons,tabGroups}, scope?{scope,chars,reason?}, prepareResult?, scriptResult?, truncated?, fullTextPath? }` rendered with a header (scope, what was expanded or what stayed collapsed as NOTEs)

**Notes:** Two modes: `url` without `windowId` reads in an isolated pooled reader (never a chat window); `windowId`, or no `url`, reads the chat's window. WebKit extracts only rendered text, so the tool runs in-page steps around the extraction (page-read.mjs): `prepare` → `expand` (details.open, aria-expanded accordions outside nav/header/footer, tab groups clicked through and appended as "Hidden tab panels") → scope (`section` / `selectors` / `scope`; isolated pages are edited in place, windows are hidden-and-restored) → `markHeadings` → extraction → probe of what stayed collapsed (a NOTE in the header) → `script` → `clean`. Isolated defaults: expand on, scope auto (main landmark when ≥ 60% of the text), headings marked, markdown cleaned; window defaults: expand off, scope page. The server's `maxWordsPerParagraph` default of 15 truncates prose, so the tool defaults it to 2000 and always extracts `region: entire_page`. Output beyond `safari.reader.maxChars` is truncated and the full text saved to a temp file.

### `safari_get_page_structure`

Outline of a web page without its text: title, text size, main-content landmark, all landmarks (header/nav/main/aside/footer/forms) and every heading with its level and a CSS selector, plus what is collapsed (closed <details>, aria-expanded="false" buttons, tab groups and their tabs). ~1–2 kB. Use it to pick a section or selectors for safari_get_page_content instead of reading a long page whole. Same two modes as safari_get_page_content (url → isolated reader; windowId → this chat's window).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string |  | URL to read. Without windowId this uses an isolated reader. |
| `windowId` | string |  | Read this chat's Safari window (s:<session>:<window>) instead of an isolated reader. Omit windowId AND url to read this session's single open window. |
| `waitMs` | number |  | Extra wait after load before extracting, for lazily rendered pages (default 0). |

**Returns:** `{ mode, windowId?, structure: { title, url, chars, main?, landmarks[], headings[{level,text,selector,hidden?,chrome?}], collapsed{details,detailsTotal,buttons,tablists[]}, forms, iframes, links } }` rendered as an outline

**Notes:** One in-page script (no extraction): landmarks and headings with CSS selectors (`#id` when unique, else an `nth-of-type` path), the main landmark and its share of the text, collapsed `<details>` / aria-expanded buttons / tab groups. Headings inside nav/header/footer are counted but omitted; hidden headings are tagged `[collapsed]`.

### `safari_wait_for`

Wait until any of the given texts appears in this chat's Safari page (polls the page text). Returns which text matched, or a timeout notice.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `text` | array of string | yes | Texts; resolves when any appears. |
| `timeout` | number |  | Milliseconds to wait (default 10000). |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text: `Found "…"` or a timeout notice

**Notes:** No server counterpart: an in-page polling script (100 ms) run in 20 s slices, because Apple's script timeout is 30 s.

### `safari_get_youtube_notes`

Show notes of a YouTube video: title, channel, duration, views, publish date, chapters (parsed from timestamps), links and the FULL description (which the rendered page never shows). Reads the watch page in an isolated Safari reader; this chat's windows are untouched. Accepts watch / youtu.be / shorts / embed URLs or a bare 11-character id.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string | yes | YouTube video URL (any form) or video id. |

**Returns:** `{ url, videoId, title, author, channelId, publishDate, category, lengthSeconds, viewCount, isLive, keywords[], chapters[{time,title}], links[], description }`

**Notes:** The rendered watch page never contains the full description (collapsed behind "…more"); the tool reads `videoDetails` from the inline `ytInitialPlayerResponse` script in an isolated reader and parses chapters from timestamp lines.

### `chrome_navigate`

Navigate this chat's Chrome window: load a url, or go back / forward / reload. Waits for the navigation to complete.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string |  | URL to load (type url). |
| `type` | `url` \| `back` \| `forward` \| `reload` |  | Navigation kind (default url). |
| `ignoreCache` | boolean |  | For reload: bypass the cache. |
| `timeout` | number |  | Milliseconds to wait for the navigation (0 = no timeout). |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]`

### `chrome_snapshot`

Text snapshot of this chat's Chrome page from the accessibility tree, listing elements with their uid. Prefer this over screenshots for reading and for finding elements; use the uids with chrome_click / chrome_fill / chrome_hover / chrome_get_screenshot. Always take a fresh snapshot after the page changes.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `verbose` | boolean |  | Include all accessibility properties (default false). |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_get_page_content`

Read a web page with Chrome and return its content as markdown | plainText | html (default markdown; the plugin's own DOM serializer — chrome-devtools-mcp has no text extractor; chrome_snapshot gives the accessibility tree instead). TWO MODES like safari_get_page_content: url without windowId reads in a temporary page of this chat's Chrome instance; windowId, or no url, reads that window (url, if also given, is loaded there first). Only RENDERED text is extracted: closed <details>, collapsed accordions and unselected tab panels are omitted unless expand is on (default for url reads; the header reports what was expanded or what stayed collapsed). Narrow with section (one heading's section), selectors (CSS) or scope: "main"; chrome_get_page_structure lists headings/selectors. prepare runs BEFORE extraction, script AFTER (its value is scriptResult).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string |  | URL to read. Without windowId this uses a temporary page of this chat's Chrome instance (opened and closed for the read; the chat's windows are untouched). |
| `windowId` | string |  | Read this chat's Chrome window (c:<session>:<window>) instead. Omit windowId AND url to read this session's single open window. |
| `waitMs` | number |  | Extra wait after load before extracting, for lazily rendered pages (default 0). |
| `format` | `markdown` \| `plainText` \| `html` |  | markdown (default; headings, lists, code fences, links, tables), plainText (innerText), html (rendered body innerHTML). |
| `expand` | boolean |  | Before extracting: open every <details>, click aria-expanded="false" accordions (outside nav/header/footer, never menus), click through each tab group and append the other panels' text under "Hidden tab panels". Default true for url reads, false for windows. |
| `section` | string |  | CSS selector of ONE heading (e.g. "#troubleshooting"); returns that heading through the next heading of the same or a higher level. Get selectors from chrome_get_page_structure. |
| `selectors` | array of string |  | CSS selectors; only the matching subtrees are extracted. Temporary pages are edited in place, windows are hidden-and-restored. Not combinable with section. |
| `scope` | `auto` \| `main` \| `page` |  | "main": only the main landmark; "page": everything; "auto" (url-read default): main when it holds >= 60% of the text. Window default: page. Ignored when section/selectors are given. |
| `clean` | boolean |  | Strip empty images/links and blank runs from markdown (default true for markdown). |
| `maxWordsPerParagraph` | number |  | Truncate every paragraph beyond this many words (default 2000 = effectively none). |
| `includeURLs` | boolean |  | Include link/image URLs (default true). |
| `prepare` | string |  | JS function body run in the page BEFORE expand/scope/extraction; `return` a value to get it back as prepareResult. |
| `script` | string |  | JS function body run in the page AFTER extraction; `return` a value to get it back as scriptResult. |

**Returns:** same shape as safari_get_page_content plus `browser: "chrome"`

**Notes:** chrome-devtools-mcp has no text extractor (its snapshot is the accessibility tree), so the content is the plugin's own in-page DOM serializer (chrome-read.mjs): headings, paragraphs, lists (nested), code fences with language, inline code, bold/italic, links, images with alt, pipe tables, blockquotes; visibility via `checkVisibility()` (display/visibility/content-visibility, which is how Chrome hides closed <details>). The surrounding expand / scope / probe / clean pipeline is page-read.mjs, shared with Safari. `url` reads use a temporary page of the session's Chrome instance (opened and closed per read; the instance stays warm until the session idle timer).

### `chrome_get_page_structure`

Outline of a web page in Chrome without its text: title, text size, main-content landmark, all landmarks and every heading with its level and a CSS selector, plus what is collapsed (closed <details>, aria-expanded="false" buttons, tab groups). ~1–3 kB. Use it to pick a section or selectors for chrome_get_page_content. Same two modes as chrome_get_page_content (url → temporary page; windowId → this chat's window).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `url` | string |  | URL to read. Without windowId this uses a temporary page of this chat's Chrome instance (opened and closed for the read; the chat's windows are untouched). |
| `windowId` | string |  | Read this chat's Chrome window (c:<session>:<window>) instead. Omit windowId AND url to read this session's single open window. |
| `waitMs` | number |  | Extra wait after load before extracting, for lazily rendered pages (default 0). |

**Returns:** same shape as safari_get_page_structure plus `browser: "chrome"`

**Notes:** Same in-page outline script as safari_get_page_structure, run through chrome-devtools-mcp's `evaluate_script`.

### `chrome_wait_for`

Wait until any of the given texts appears on this chat's Chrome page.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `text` | array of string | yes | Texts; resolves when any appears. |
| `timeout` | number |  | Milliseconds (0 = no timeout). |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

## JavaScript

### `safari_evaluate_expression`

Run JavaScript statements in this chat's Safari window. `expression` is a FUNCTION BODY: use an explicit `return` for a value (await is allowed). `$uid(N)` references a node UID from safari_get_page_content. Returns the JSON-encoded result. (safari_evaluate_function takes a function + args instead.)

| Parameter | Type | Required | Description |
|---|---|---|---|
| `expression` | string | yes | JavaScript function body; `return` the value you want. |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |
| `frameId` | string |  | Node UID of an iframe (or a node inside one) to run in that subframe. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `safari_evaluate_function`

Call a JavaScript FUNCTION in this chat's Safari window, e.g. `() => document.title` or `(el) => el.innerText` with args naming node UIDs from safari_get_page_content (each resolves to that element). Async functions are awaited. Returns the JSON-encoded result.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `function` | string | yes | A function expression (arrow or function), called with args. |
| `args` | array of string |  | Node UIDs (strings) passed as element arguments, in order. |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |
| `frameId` | string |  | Node UID of an iframe to run in that subframe. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Wrapped as `return await (fn)($uid(a), $uid(b))`, so `args` are node UIDs from `safari_get_page_content`.

### `chrome_evaluate_function`

Call a JavaScript FUNCTION in this chat's Chrome page, e.g. `() => document.title` or `(el) => el.innerText` with args naming snapshot uids (each resolves to that element). Async functions are awaited. The return value must be JSON-serializable.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `function` | string | yes | A function expression (arrow or function), called with args. |
| `args` | array of string |  | Snapshot uids (strings) passed as element arguments, in order. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Server-native `evaluate_script`; `args` are snapshot uids only (plain values must be inlined into the function source).

### `chrome_evaluate_expression`

Run JavaScript statements in this chat's Chrome page. `expression` is a FUNCTION BODY: use an explicit `return` for a value (await is allowed). Returns the JSON-encoded result. (chrome_evaluate_function takes a function + uid args instead.)

| Parameter | Type | Required | Description |
|---|---|---|---|
| `expression` | string | yes | JavaScript function body; `return` the value you want. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Wrapped as `async () => { … }` for the server's `evaluate_script`.

## Interaction

### `safari_interact`

Perform DOM interactions in this chat's Safari window, in sequence (400 ms settle between each): click, type, keyPress, scroll, selectText, selectMenuItem, hover, highlightText. Target by node UID (from safari_get_page_content), by find-in-page text, or by viewport point. Batch related steps in ONE call. Returns a diff of the page text (or the full loaded page if a click navigated).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `interactions` | array of objects | yes | Steps, executed in order. |
| `interactions[].type` | `click` \| `type` \| `keyPress` \| `scroll` \| `selectText` \| `selectMenuItem` \| `hover` \| `highlightText` | yes | Interaction kind. |
| `interactions[].purpose` | string | yes | Short description of the intended outcome. |
| `interactions[].node` | string |  | Node UID. Omit to target by `text` (find-in-page). |
| `interactions[].text` | string |  | Find-in-page query, scroll search term, or menu item label. |
| `interactions[].value` | string |  | For type: text to type; for keyPress: key name. |
| `interactions[].pressReturn` | boolean |  | For type: press Return afterwards (default false). |
| `interactions[].replaceAll` | boolean |  | For type: replace existing field text (default false). |
| `interactions[].scrollToVisible` | boolean |  | Scroll the target into view first (default false). |
| `interactions[].point` | object |  | Viewport coordinates, last resort. |
| `interactions[].point.x` | number | yes |  |
| `interactions[].point.y` | number | yes |  |
| `interactions[].scrollDelta` | object |  | For scroll: pixel delta. |
| `interactions[].scrollDelta.x` | number |  |  |
| `interactions[].scrollDelta.y` | number |  |  |
| `fullText` | boolean |  | Return the full page text instead of a diff (default false). |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Direct forward of Apple's `page_interactions`. Prefer one batched call over several single-step calls.

### `safari_click`

Click an element in this chat's Safari window (by node UID, find-in-page text, or point). Waits for a triggered navigation. Returns the page diff. For several steps use safari_interact.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `node` | string |  | Node UID from safari_get_page_content (preferred). |
| `text` | string |  | Find-in-page text identifying the element when no node is known. |
| `point` | object |  | Viewport coordinates, last resort. |
| `point.x` | number | yes |  |
| `point.y` | number | yes |  |
| `scrollToVisible` | boolean |  | Scroll the target into view first (default true). |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Single-step convenience over `safari_interact` (`scrollToVisible` defaults to true). Waits for a navigation the click triggers.

### `safari_hover`

Hover an element in this chat's Safari window (by node UID, text, or point). Returns the page diff.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `node` | string |  | Node UID from safari_get_page_content (preferred). |
| `text` | string |  | Find-in-page text identifying the element when no node is known. |
| `point` | object |  | Viewport coordinates, last resort. |
| `point.x` | number | yes |  |
| `point.y` | number | yes |  |
| `scrollToVisible` | boolean |  | Scroll the target into view first (default true). |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Single-step convenience over `safari_interact`.

### `safari_press_key`

Press a key in this chat's Safari window (e.g. "Enter", "Escape", "Tab", "ArrowDown"), optionally on a target element. Returns the page diff.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `key` | string | yes | Key name. |
| `node` | string |  | Node UID from safari_get_page_content (preferred). |
| `text` | string |  | Find-in-page text identifying the element when no node is known. |
| `point` | object |  | Viewport coordinates, last resort. |
| `point.x` | number | yes |  |
| `point.y` | number | yes |  |
| `scrollToVisible` | boolean |  | Scroll the target into view first (default true). |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Single-step convenience over `safari_interact` (`keyPress`).

### `safari_type_text`

Type text into a field in this chat's Safari window (target by node UID or find-in-page text), optionally replacing existing text and/or pressing Return to submit. Returns the page diff.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes | Text to type. |
| `node` | string |  | Node UID from safari_get_page_content (preferred). |
| `target` | string |  | Find-in-page text identifying the field when no node is known. |
| `replaceAll` | boolean |  | Replace existing field text (default false). |
| `pressReturn` | boolean |  | Press Return after typing (default false). |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Single-step convenience over `safari_interact` (`type`, with `replaceAll` / `pressReturn`).

### `chrome_click`

Click an element (uid from chrome_snapshot) in this chat's Chrome page.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `uid` | string | yes | Element uid. |
| `dblClick` | boolean |  | Double-click. |
| `includeSnapshot` | boolean |  | Return a fresh snapshot afterwards. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_fill`

Type into an input / textarea / contenteditable or choose a select option (uid from chrome_snapshot).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `uid` | string | yes | Element uid. |
| `value` | string | yes | Value to fill. |
| `includeSnapshot` | boolean |  |  |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_fill_form`

Fill several form fields at once (uids from chrome_snapshot).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `elements` | array of objects | yes | Fields to fill. |
| `elements[].uid` | string | yes |  |
| `elements[].value` | string | yes | "true"/"false" for checkboxes and toggles. |
| `includeSnapshot` | boolean |  |  |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_hover`

Hover an element (uid from chrome_snapshot).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `uid` | string | yes |  |
| `includeSnapshot` | boolean |  |  |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_press_key`

Press a key or combination in this chat's Chrome page, e.g. "Enter", "Escape", "Control+a".

| Parameter | Type | Required | Description |
|---|---|---|---|
| `key` | string | yes | Key or combination. |
| `includeSnapshot` | boolean |  |  |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_type_text`

Type text at the current focus in this chat's Chrome page (focus an element first with chrome_click), optionally followed by a key such as Enter.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `submitKey` | string |  | Key to press after typing, e.g. Enter. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_interact`

Perform DOM interactions in this chat's Chrome page in sequence, mirroring safari_interact: click, type, keyPress, scroll, hover, selectMenuItem (selectText/highlightText and point targets are not supported in Chrome and are reported as failed steps). Target by snapshot uid (`node`) or by page text (`text`). Steps continue after a failed step; the result reports each step's outcome.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `interactions` | array of objects | yes | Steps, executed in order. |
| `interactions[].type` | `click` \| `type` \| `keyPress` \| `scroll` \| `selectText` \| `selectMenuItem` \| `hover` \| `highlightText` | yes | Interaction kind. |
| `interactions[].purpose` | string | yes | Short description of the intended outcome. |
| `interactions[].node` | string |  | Snapshot uid of the target. |
| `interactions[].text` | string |  | Page text identifying the target (for selectMenuItem: the option label). |
| `interactions[].value` | string |  | For type: text to type; for keyPress: key or combination. |
| `interactions[].pressReturn` | boolean |  | For type: press Enter afterwards. |
| `interactions[].replaceAll` | boolean |  | For type: replace the field's existing text (default: fill replaces when a uid is given). |
| `interactions[].scrollToVisible` | boolean |  | Scroll the target into view first. |
| `interactions[].scrollDelta` | object |  | For scroll: pixel delta. |
| `interactions[].scrollDelta.x` | number |  |  |
| `interactions[].scrollDelta.y` | number |  |  |
| `includeSnapshot` | boolean |  | Append a fresh snapshot after the batch (default false). |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text: one line per step (`ok` / `FAILED: reason`), optional snapshot

**Notes:** Mirrors `safari_interact`'s batch format on Chrome: click/hover/keyPress/type/scroll/selectMenuItem map onto `click`/`hover`/`press_key`/`fill`+`press_key`/`type_text`/`evaluate_script`/`fill`. `node` = snapshot uid (native); `text` targets use an in-page finder (deepest visible element containing the text). `selectText`, `highlightText` and `point` targets are reported as failed steps; the batch continues and each step's outcome is listed.

## Screenshots

### `safari_get_screenshot`

Screenshot of this chat's Safari window, returned INLINE as an image. With querySelector, only that element: scrolled into view (scrollTo), settled, measured, captured, re-measured (re-captured once if it moved) and cropped at device-pixel precision. Without: the viewport, or the whole page with fullPage.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |
| `querySelector` | string |  | CSS selector of one element to capture (document.querySelector). Omit for the whole viewport. |
| `scrollTo` | boolean |  | With querySelector: scroll the element into view (centered) and wait for scrolling to settle first (default true). |
| `fullPage` | boolean |  | Without querySelector: capture the entire scrollable page instead of the viewport (default false). |

**Returns:** inline image + `{ windowId, width, height, rect?, viewport?, scale?, clipped?, settled?, unstable? }`

**Notes:** With `querySelector`: scroll into view (`scrollTo`), wait until rect and scroll offset are stable for three samples (setTimeout polling — rAF does not fire in occluded windows), capture the viewport, re-measure (retake once if it moved > 2 px), crop with sharp at device-pixel precision. Inline images go through DSH's attachment store when the current model declares image input; otherwise a temp-file path is returned.

### `safari_save_screenshot`

Screenshot of this chat's Safari window written to a PNG file (same element capture options as safari_get_screenshot). Returns path and pixel size.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `path` | string | yes | Destination .png path (absolute, or relative to the session workspace). |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |
| `querySelector` | string |  | CSS selector of one element to capture (document.querySelector). Omit for the whole viewport. |
| `scrollTo` | boolean |  | With querySelector: scroll the element into view (centered) and wait for scrolling to settle first (default true). |
| `fullPage` | boolean |  | Without querySelector: capture the entire scrollable page instead of the viewport (default false). |

**Returns:** `{ path, width, height, … }`

**Notes:** Same capture as `safari_get_screenshot`, written to `path` (relative paths resolve against the session workspace; parents are created).

### `chrome_get_screenshot`

Screenshot of this chat's Chrome page, returned INLINE as an image: the viewport, the whole page (fullPage), or one element (uid from chrome_snapshot; uids expire when the page changes — re-snapshot first). Large captures are handled transparently; format: "jpeg" or a uid crop keeps them small.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |
| `uid` | string |  | Element uid from chrome_snapshot to capture just that element. |
| `fullPage` | boolean |  | Capture the whole scrollable page (default false). |
| `format` | `png` \| `jpeg` \| `webp` |  | Image format (default png). |
| `quality` | number |  | jpeg/webp quality 0–100. |

**Returns:** inline image + `{ windowId, width?, height?, uid?, fullPage }`

**Notes:** Server-native capture (viewport, `fullPage`, or element `uid`); inline via the attachment store under the same admission rule as `safari_get_screenshot`.

### `chrome_save_screenshot`

Screenshot of this chat's Chrome page written to a file (viewport, fullPage, or element uid). Returns the path.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `path` | string | yes | Destination path (absolute, or relative to the session workspace); extension should match format. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |
| `uid` | string |  | Element uid from chrome_snapshot to capture just that element. |
| `fullPage` | boolean |  | Capture the whole scrollable page (default false). |
| `format` | `png` \| `jpeg` \| `webp` |  | Image format (default png). |
| `quality` | number |  | jpeg/webp quality 0–100. |

**Returns:** `{ path, … }`

**Notes:** Same capture as `chrome_get_screenshot`, written to `path`.

## Diagnostics, dialogs and viewport

### `safari_console_messages`

Console messages (log/info/warn/error) buffered for this chat's Safari window (needs a loaded page). The buffer is kept unless clear is true.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |
| `limit` | number |  | Maximum number of messages (default 100, max 500). |
| `clear` | boolean |  | Clear the buffer after reading (default false). |
| `level_filter` | array of `log` \| `info` \| `warn` \| `error` \| `debug` |  | Only these levels. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Apple's server clears the buffer by default; this tool keeps it unless `clear` is true, matching Chrome.

### `safari_network_requests`

Network requests recorded in this chat's Safari window (method, URL, status, timing, request_id for safari_get_network_request). Recording starts with the FIRST call: call it once, then navigate or reload, then call again. Needs a loaded page.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |
| `clear` | boolean |  | Clear the recorded list after reading. |
| `since` | string |  | Only requests after this ISO timestamp. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Inspector recording is enabled by the first call (verified: 0 requests for a page loaded before it, 1 after the next navigation). Diagnostics tools require a loaded page ("No active browsing context" otherwise).

### `safari_get_network_request`

Full detail of one network request recorded in this chat's Safari window (headers, text body within the size cap, timing), by the request id from safari_network_requests.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `request_id` | string | yes | Request id from safari_network_requests. |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `safari_handle_dialog`

List or answer a JavaScript dialog (alert/confirm/prompt) in this chat's Safari window. An open dialog blocks every other tool until it is answered.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `action` | `list` \| `accept` \| `dismiss` | yes | list = report open dialogs; accept = OK (with text for prompts); dismiss = Cancel. |
| `text` | string |  | Text to enter for a prompt dialog when accepting. |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** `accept` maps to Apple's `respond`; `list` reports open dialogs.

### `safari_set_viewport_size`

Resize this chat's Safari window viewport (CSS pixels).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `width` | number | yes | Viewport width in CSS px. |
| `height` | number | yes | Viewport height in CSS px. |
| `windowId` | string |  | Window id (s:<session>:<window>) from safari_open. Omit when this session has at most one safari window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_console_messages`

Console messages of this chat's Chrome page (paginated).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `types` | array of `log` \| `debug` \| `info` \| `error` \| `warn` \| `dir` \| `dirxml` \| `table` \| `trace` \| `clear` \| `startGroup` \| `startGroupCollapsed` \| `endGroup` \| `assert` \| `profile` \| `profileEnd` \| `count` \| `timeEnd` \| `issue` |  | Only these message types. |
| `pageSize` | number |  |  |
| `pageIdx` | number |  |  |
| `includeStackTraces` | boolean |  |  |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_network_requests`

Network requests of this chat's Chrome page (paginated; filter by resource type).

| Parameter | Type | Required | Description |
|---|---|---|---|
| `resourceTypes` | array of string |  | Only these resource types (e.g. document, xhr, fetch, script, image). |
| `pageSize` | number |  |  |
| `pageIdx` | number |  |  |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_get_network_request`

Full detail of one network request of this chat's Chrome page (headers, body, timing), by the reqid from chrome_network_requests.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `reqid` | number | yes | Request id from chrome_network_requests. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

### `chrome_handle_dialog`

Answer a JavaScript dialog (alert/confirm/prompt) open in this chat's Chrome page. An open dialog blocks every other tool until it is answered.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `action` | `accept` \| `dismiss` | yes | accept = OK (with text for prompts); dismiss = Cancel. |
| `text` | string |  | Text to enter for a prompt dialog when accepting. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** `text` maps to the server's `promptText`.

### `chrome_set_viewport_size`

Resize this chat's Chrome page to the given CSS pixel size.

| Parameter | Type | Required | Description |
|---|---|---|---|
| `width` | number | yes | Page width in CSS px. |
| `height` | number | yes | Page height in CSS px. |
| `windowId` | string |  | Window id (c:<session>:<window>) from chrome_open. Omit when this session has at most one chrome window: it is used, or one is opened. |

**Returns:** text from the server, prefixed `[windowId]` (and `Opened …` when a window was auto-opened)

**Notes:** Forwards to `resize_page`.
